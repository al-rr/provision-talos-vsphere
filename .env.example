# Legacy compatibility sample only.
# The active configuration model is:
#   1. overlays/base/scripts/vars.sh
#   2. overlays/<env>/scripts/vars.sh
#
# Keep root .env usage disabled by default. Only use it temporarily when a
# compatibility shim or local legacy workflow still requires it.

OVERLAY_ALLOW_LEGACY_ENV="false"
VSPHERE_ENDPOINT=""
VSPHERE_USERNAME=""
VSPHERE_PASSWORD=""
HAPROXY_VIP=""
TALOS_CLUSTER_NAME="talos-prod"