# Canonical Packer Layout

This directory defines the long-term canonical layout by distribution and version.

Current build backends (`vsphere-iso`, `vmware-iso`) are still active while
templates converge to this structure.
