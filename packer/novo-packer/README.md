# Deprecated Directory

This directory was a temporary copy used during migration.

The active and unified implementation now lives in:
- `packer/vsphere-iso/`

Use:
- `packer/vsphere-iso/build.sh`
- `packer/vsphere-iso/README.md`
- `packer/vsphere-iso/examples/vsphere.env.example`

Rationale:
- avoids duplicate Packer definitions
- centralizes variables and execution flow
- reduces maintenance and drift
